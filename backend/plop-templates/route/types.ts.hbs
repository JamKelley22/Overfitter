import { ClothingItem } from "../../types";

import { {{typeName}}Type, {{typeName}} as I{{typeName}} } from "../../../docs/client/api";

export class {{typeName}} extends ClothingItem implements I{{typeName}} {
    type: {{typeName}}Type;

    constructor(data: I{{typeName}}) {
        super(data);
        this.type = data.type ?? {{typeName}}Type.Unknown;
    }

    toString() {
        return super.toString(`type: ${this.type}`);
    }
}

export { I{{typeName}}, {{typeName}}Type };
